export class WebCanvasUniverse {
  constructor(containerElement) {
    this.container = containerElement;
    // 3D mode completely removed
  }

  playEntrance() {
    // No 3D entrance
  }

  setScrollProgress(progress) {
    // No 3D scroll progress
  }

  destroy() {
    // Cleanup
  }
}
